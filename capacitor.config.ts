import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spendlist.app',
  appName: 'Spend List',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
